{
  "name": "cypress-cucumber-example",
  "version": "0.0.0",
  "description": "E2E cypress-cucumber-example",
  "scripts": {
    "types": "tsc --noEmit",
    "clean": "npx rimraf ./report ./downloads",
    "cypress:open": "npm run clean && cypress open",
    "cypress:run": "npm run clean && cypress run"
  },
  "devDependencies": {
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "4.7.4"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.1.4",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
    "cucumber-json-report-formatter": "^0.1.4",
    "cypress": "^12.7.0"
  }
}
